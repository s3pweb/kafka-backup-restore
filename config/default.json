{
  "name": "kafka-backup-restore",
  "kafka": {
    "consumer": {
      "config": {
        "metadata.broker.list": "kafka:9092",
        "group.id": "kafka-backup-restore.group"
      }
    },
    "producer": {
      "config": {
        "metadata.broker.list": "kafka:9092"
      },
      "topicsPrefix": "s3pweb."
    }
  },
  "consoleLogLevel": "trace"
}
